spring.application.name=hipatec


spring.datasource.url=jdbc:sqlserver://hipatech.database.windows.net:1433;database=hipatec;user=adm@hipatech;password=Hip4tech;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
spring.datasource.username=adm
spring.datasource.password=Hip4tech

spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

cloudinary.cloud_name=dsxbc33ui
cloudinary.api_key=261759412581626
cloudinary.api_secret=lLAXdOL8CtrPA_8rSaarWCyr2eU

# Recuperação de senha: SMTP local por padrão, sem credenciais versionadas.
spring.mail.host=${SMTP_HOST:localhost}
spring.mail.port=${SMTP_PORT:1025}
spring.mail.username=${SMTP_USERNAME:}
spring.mail.password=${SMTP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${SMTP_AUTH:false}
spring.mail.properties.mail.smtp.starttls.enable=${SMTP_STARTTLS:false}
spring.mail.properties.mail.smtp.starttls.required=${SMTP_STARTTLS:false}
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
hipatec.mail.from=${HIPATEC_MAIL_FROM:nao-responda@hipatec.local}
hipatec.frontend-origin=${HIPATEC_FRONTEND_ORIGIN:http://localhost:8100}
hipatec.recuperacao-senha.url=${HIPATEC_RESET_URL:http://localhost:8100/redefinir-senha}

# smtp (Mailpit local) ou onesignal (API HTTPS). Segredos somente no ambiente.
hipatec.mail.provider=${HIPATEC_MAIL_PROVIDER:smtp}
hipatec.onesignal.app-id=${ONESIGNAL_APP_ID:}
hipatec.onesignal.api-key=${ONESIGNAL_API_KEY:}
